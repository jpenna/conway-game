describe('Store: game', () => {
  describe('actions', () => {
    it('initConnection: should commit CONNECTED type');
  });
  describe('mutations', () => {
    it('CONNECTED: should set connected flag = true');
  });
});
