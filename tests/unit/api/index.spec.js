describe('API', () => {
  it('setStore: sets store in `socket` instance');
  it('replaces websocket send method to always send JSON');
  it('On open connection, dispatch initConnection action to Vuex store');
});
